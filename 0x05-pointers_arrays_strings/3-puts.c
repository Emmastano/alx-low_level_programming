#include "main.h"

/**
 * _3-puts -  function that prints a string, followed by a new line, to stdout
 * @str: string to print
 * Reurn: always 0
 */

void_puts(char *str)
{
	int 1 = 0;

	while (*(str + i) != '\0')
	{
		putchar(*(str + i));
		i++;
	}

	putchar('\n')
}
